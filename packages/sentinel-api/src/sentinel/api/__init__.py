"""The dashboard API.

Reads the database for map, search, route and traffic views. Serves the
frontend build. In the evaluation topology this process also holds
correlation, which is why it lives next to it in the workspace.
"""
